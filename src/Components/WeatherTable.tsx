import React from 'react'
import {
  Table,
  TableHead,
  TableBody,
  TableRow,
  TableCell,
  TableSortLabel
} from '@material-ui/core'
import { CityData, DailyInfo } from '../types'

const WeatherTable = () => {
  
}